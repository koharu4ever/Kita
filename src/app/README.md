# `src/app`

Next.js App Router lives here.

Keep route files focused on routing, layout, metadata, route handlers, and
server component composition. Move business behavior into `features` or
server-only integrations into `server` when they grow.
