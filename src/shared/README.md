# `src/shared`

Cross-feature reusable code lives here.

Suggested subfolders:

```txt
components/
hooks/
lib/
types/
```

Only move code here after it is truly shared. This keeps `shared` from becoming
a catch-all folder.
